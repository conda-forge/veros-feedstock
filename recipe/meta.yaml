{% set name = "veros" %}
{% set version = "1.5.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/veros-{{ version }}.tar.gz
  sha256: c4f7e127727093d13c82beac0fdb31a6421692f606ef242c0eeebc465b7cb21d

build:
  skip: true  # [py<37]
  entry_points:
    - veros = veros.cli.veros:cli
    - veros-run = veros.cli.veros_run:cli
    - veros-copy-setup = veros.cli.veros_copy_setup:cli
    - veros-resubmit = veros.cli.veros_resubmit:cli
    - veros-create-mask = veros.cli.veros_create_mask:cli
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
  host:
    - python
    - setuptools >=65.0.0
    - wheel
    - cython
    - pip
  run:
    - python
    - click <=8.1.7
    - entrypoints <=0.4
    - requests <=2.31.0,>=2.18
    - numpy <=1.25.2,>=1.13
    - scipy <=1.11.2
    - h5netcdf <=1.2.0
    - h5py <=3.9.0
    - pillow <=10.0.0
    - loguru <=0.7.0
    - tqdm <=4.66.1

test:
  imports:
    - veros
  commands:
    - pip check
    - veros --help
    - veros-run --help
    - veros-copy-setup --help
    - veros-resubmit --help
    - veros-create-mask --help
  requires:
    - pip

about:
  home: https://veros.readthedocs.io
  summary: The versatile ocean simulator, in pure Python, powered by JAX
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - ocefpaf
